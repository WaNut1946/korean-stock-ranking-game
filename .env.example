PORT=4000
CLIENT_ORIGIN=http://localhost:5173
VITE_API_URL=http://localhost:4000
JWT_SECRET=change-this-secret

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=stock_game

# Set to memory when MySQL is not ready yet.
DATA_STORE=mysql

# mock now, replace with a real provider later.
PRICE_PROVIDER=mock
